<?php

return [
  // this is done so that the path of the routes can be changed
  // easily through constants instead of changing everything
  // individually if needed
  'routes' => [
    'secret' => 'secret',
    'api' => 'api',
    'auth' => 'auth',
  ],
];
